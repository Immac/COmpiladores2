#include "number-expression.h"

#include <string>
using namespace std;

string NumberExpression::generateCode() {
	return "";
}

string NumberExpression::getLocation(){
	return to_string(this->value);
}